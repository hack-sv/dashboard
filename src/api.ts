// Auth endpoints live under /auth/* (the WorkOS Google callback is /auth/callback).
// Built assets live under /dashboard/* (see vite.config.ts). Both are within the
// two reserved route prefixes on the hack.sv zone.
export const API_BASE = '/auth'
